# Django Authentication System Requirements
# Core Django framework
Django>=5.1.3,<6.0

# Additional packages that might be useful for authentication systems
# Email backend support (for password reset emails)
# If using Gmail SMTP, no additional packages needed as Django has built-in support

# For production deployments (optional)
# psycopg2-binary>=2.9.0  # For PostgreSQL
# mysqlclient>=2.1.0      # For MySQL
# gunicorn>=20.1.0        # WSGI server for production

# Development dependencies (optional)
# python-decouple>=3.6    # For environment variable management
# pillow>=9.0.0           # For image handling if adding profile pictures
